# 宝塚市民向け情報サイト (CLAUDE.md)

このファイルはこのプロジェクト専用の設定です。
（グローバル設定 `~/.claude/CLAUDE.md` の内容も併せて適用されます）

## サイトの目的

宝塚市の行政・暮らし情報をまとめた、宝塚市民向け情報サイト。

## 技術方針

- **静的HTML / CSS / JavaScript** で構築する（データベース・管理画面は使わない）。
- HTML生成は**素のNode.jsスクリプト**で実装する（外部の静的サイトジェネレーター・フレームワークは使わない）。
- **Cloudflare Pages** で無料公開できる構成にする。
- **スマホ対応**（レスポンシブデザイン）必須。
- **SEO対応**必須（meta タグ、構造化データ、sitemap.xml など）。
- 記事は自動生成してHTML化する。
- 維持費ゼロ〜低額、月間10万PV程度まで耐えられる構成を想定する。
- 将来的にCMS化・データベース化を検討する可能性はあるが、現時点では実装しない（指示があるまでは静的サイト構成のまま）。

## 運用ルール

- 自動生成された記事は、当面は**公開前に人間レビューを挟む**（例：Pull Requestでの確認後にmainへマージして公開）。完全自動公開には切り替えない（指示があるまで）。
- 各情報源（市公式サイト・議会・広報たからづか・兵庫県・警察防犯情報・イベント情報）からのデータ取得を実装する前に、**利用規約・robots.txtを全ソース調査してから着手する**。調査未了のソースの実装を先行させない。

### 情報源ごとの調査結果（2026-06-21時点）

| 情報源 | 状況 | 注意点 |
|---|---|---|
| 宝塚市公式サイト | 調査済み・着手可 | robots.txtは`/cgi-crm/`のみ禁止。転載規定の明記なしのため、原文転載禁止ルールは厳守 |
| 広報たからづか | 調査済み・着手可 | 「PDF・電子書籍データの改変・コピー・再配布等の二次利用不可」と明記。要約＋出典リンクのみで対応 |
| 兵庫県公式発表 | **未確認**（robots.txt取得失敗） | 著作権は原則県に帰属、無断転用禁止。実装着手前にrobots.txt再調査が必要 |
| 警察防犯情報 | **未確認**（robots.txt取得失敗） | 出典記載必須、無断複製・転用禁止。個人情報（被疑者・被害者等）混入リスクが高く特に注意。実装着手前にrobots.txt再調査が必要 |
| 宝塚市議会（会議録） | **未確認**（gijiroku.com側の規約取得失敗） | 市公式サイト経由分はOKだが、別ドメインのgijiroku.com部分は要再調査。発言者の個人名（参考人等）に注意 |
| イベント情報 | **未確認**（観光協会サイトのrobots.txt取得失敗） | 著作権者が市・観光協会以外の共催団体になる場合があり個別確認が必要 |

→ 当面は**宝塚市公式サイト・広報たからづか**から実装に着手する。残り4ソースは規約調査が完了するまで実装しない。

## データ取得方法（確定方針）

### 宝塚市公式サイト（お知らせ）
- 取得元：`https://www.city.takarazuka.hyogo.jp/news.rss`（RSS 2.0）
- 取得頻度：1日1回
- **カテゴリで事前フィルタする**：採用・入札等の定時的・事務的コンテンツは除外し、市民の暮らしに関わる情報のみ記事化する
- 差分判定：`link`のURLで重複チェック

### 広報たからづか
- 取得元：月号一覧ページ → 各号の面別PDF
- 取得頻度：月号更新の確認は毎日、実際の更新は月1回程度
- **記事化する範囲は「特集・トピックス・イベント情報」面のみ**（委員募集・入札・税金のお知らせ等の事務的な面は記事化しない）
- PDFは二次利用不可のため、テキスト抽出後にPDF自体は保存しない（一時処理のみ）

両ソース共通で、フィルタ条件（除外カテゴリ／対象面）は実装時にキーワード・面タイトルでリスト化し、運用しながら調整する。

## 依存ライブラリ（許可済み）

- `rss-parser`：お知らせ（RSS）のXML解析に使用
- PDFテキスト抽出ライブラリ（`pdf-parse`等）は**未導入**。広報たからづかの実装着手時に改めて確認する
- これ以外のライブラリ追加は、グローバル設定のルール通り、その都度指示を確認する

## 実装の進め方（確定方針）

- まずは**宝塚市公式サイトのお知らせ（RSS）パイプラインのみ**を実装する
- 広報たからづか（PDF解析）は後回し。着手時にPDF抽出ライブラリの導入を改めて相談する

## UI/UXデザイン方針（2026-06-21刷新）

- デザインは「Yahooニュース＋地域メディア（神戸新聞NEXT等）風」（3案中の案3）を採用
- アイコンは絵文字を使わず、`scripts/templates.js`内の自作インラインSVGアイコンセット（線画・モダン）を使用。外部アイコンフォント・アイコンライブラリは導入しない
- ダークモード対応：`<html data-theme>` 属性＋`localStorage`で永続化。初期化スクリプトを`<head>`にインラインで置き、画面のちらつき（FOUC）を防止
- カテゴリーは5分類で固定：行政・くらし／防犯・安全／イベント／子育て／文化・観光
- 「今日の宝塚トピック」は当日（Asia/Tokyo基準）公開記事を自動表示
- 「人気記事ランキング」は`data/ranking.json`の`weekly`配列が5件以上になるまで非表示。未蓄積時は「新着記事」パネルで代替（実データのない人気表示は誇張・推測にあたるため）
- ローカルでの見た目確認は`npm run preview`（Node標準機能のみの簡易HTTPサーバー、`scripts/preview-server.js`）を使う。`file://`で直接HTMLを開くと絶対パス（`/css/style.css`等）が解決できずCSSが効かないため注意
- デザイン案の比較検討時に作成したモック（`mockups/`配下）は確認用。本番のテンプレートとは別管理

## Cloudflare Web Analytics（2026-06-21導入済み）

- pages.devドメインはCloudflare DNS管理外のため自動ビーコン注入は使えず、**JSスニペットを手動で`scripts/templates.js`の`layout()`に埋め込む方式**を採用
- ビーコンのトークンはサイトhttps://takarazuka-citizen-info.pages.dev に紐づく公開用トークン（クライアント側に埋め込む前提のものでシークレットではない）
- 人気記事ランキングの実データ化（`data/ranking.json`の`weekly`/`monthly`生成）は**未実装**。アクセスデータが蓄積された後、Cloudflare GraphQL Analytics API等の集計方法を改めて相談する

## Google Search Console（2026-06-21導入済み）

- プロパティ：URLプレフィックス方式で`https://takarazuka-citizen-info.pages.dev`を登録（pages.devは自分のドメインではないためドメインプロパティ・DNS認証は使えない）
- 所有権確認：HTMLタグ方式。`<meta name="google-site-verification">`を`scripts/templates.js`の`layout()`に埋め込み済み
- サイトマップ送信済み：`sitemap.xml`（`lastmod`付き）
- 独自ドメインに移行した場合は、Search Consoleのプロパティを再登録（ドメインプロパティへの切替）が必要

## SEO基盤（2026-06-21強化）

- OGP（`og:title`/`og:description`/`og:url`/`og:type`/`og:site_name`/`og:locale`）とTwitterカード（`summary`）を`layout()`に追加
- トップページに`WebSite`構造化データ（JSON-LD）、記事ページは既存の`NewsArticle`構造化データを継続
- ファビコンは外部画像ツールを使わず`assets/favicon.svg`（自作インラインSVG、建物アイコン＋ブランドカラー）を追加
- og:image（SNSシェア用画像）は未設定。画像生成ツールがないため未対応。必要になったら方針を相談する

## フェーズ2：検索流入強化（2026-06-21）

### カテゴリー拡張
- 生成・公開対象カテゴリ8分類：行政・くらし／防犯／防災／イベント／子育て／教育／文化・観光（`scripts/templates.js`の`CATEGORIES`）
- **市議会**はUIのみ先行追加（カテゴリーチップに「準備中」バッジ、`/category/shigikai.html`は固定の準備中メッセージを表示）。**議会会議録サイト(gijiroku.com)の規約調査が完了するまで、データ取得・要約・公開は一切行わない**（[[CLAUDE.md内の情報源調査結果]]の方針を優先）
- カテゴリーごとに固有URL（`/category/<key>.html`）のアーカイブページを生成。SEO上のインデックス対象を増やす目的と、Bランク記事の「一覧のみ」要件を同時に満たす

### 記事価値スコア（S/A/B/C）
- 全記事のJSONに`valueScore`（"S"|"A"|"B"|"C"）を付与する
- 公開範囲：**S・A＝通常公開**（トップニュース・今日のトピック・カテゴリープレビューに表示）／**B＝一覧のみ**（カテゴリーアーカイブページ・新着記事一覧には表示するが、トップの目立つ枠には出さない）／**C＝非公開**（HTML生成・サイトマップ掲載とも対象外。収集記録として`data/articles/`に残すのみ）
- 評価基準：
  - **S（最優先・即公開）**：税金変更／ゴミ収集変更／学校統廃合／給食費変更／大規模工事／災害情報／防犯情報／公共交通変更／市議会重要決定 など、市民生活に直接影響する情報
  - **A（公開）**：市主催イベント／子育て支援／教育施策／公共施設開設／健康福祉制度変更／市予算の主要項目 など、多くの市民に関係する情報
  - **B（一覧のみ）**：特定世代向け制度／地域限定イベント／各種募集／補助金案内 など、一部市民向け情報
  - **C（非公開）**：入札公告／審議会委員募集／業者向け案内／会議開催通知／事務手続きのみの記事 など、行政事務情報
- 記事数目標は**撤廃**。「Sは必ず公開、Aは公開、量より質」を優先し、1日0〜10記事になることを許容する
- 将来、記事不足が続く場合は兵庫県・兵庫県警・市議会・防災情報のソース追加（規約確認後）や、ワールドカップ日本代表戦のような全市民共通の話題的トピックの特別枠を検討する

### SNS連携準備（下書きのみ・自動投稿なし）
- 各記事JSONに`sns: { x, instagram, youtube_shorts }`フィールドを追加し、記事と同じJSONファイルに保存する
- 自動投稿APIとの連携は未実装。当面は人間がこの内容をコピペして手動投稿する
- SNS文面も記事生成ルール（事実のみ・誇張禁止・原文転載禁止・出典明記）に従う

### SEO改善（2026-06-21再検証で追加）
- 記事ページ・カテゴリーページに`BreadcrumbList`構造化データ（JSON-LD）を追加
- `layout()`の`structuredData`は配列を渡すことで複数のJSON-LDブロックを出力できるよう拡張
- カテゴリーアーカイブページの新設自体もSEO改善の一環（固有URL＝インデックス対象の増加）

## 開発環境

- OS: Windows 11 / シェル: PowerShell（グローバル設定と同様）
- デプロイ先: Cloudflare Pages
- 公開URL: https://takarazuka-citizen-info.pages.dev （独自ドメイン取得時は要更新）
- GitHubリポジトリ: https://github.com/tanikku/takarazuka-citizen-info
- Cloudflare Pages設定: Production branch=`main` / Build command=`npm run build` / Build output directory=`public`
- デプロイフロー: `main`ブランチへのpushでCloudflare Pagesが自動ビルド・デプロイ

## 記事生成ルール

- 事実のみ記載する。推測は禁止。
- 誇張した表現は禁止。
- 原文の転載は禁止（要約・言い換えで記載する）。
- 出典URLを必須で記載する。
- 文字数は300〜500文字程度。
- 市民向けにわかりやすく説明する。
- 「何が変わるか」を最優先で説明する。

## 注意事項

- データベース・管理画面など、現時点で不要な仕組みを先回りして作らない。
- 記事生成・更新の自動化を行う際は、生成ロジックと出力されるHTMLの構成を明確に分離する。
- ライブラリ追加やフレームワーク導入は、グローバル設定のルール通り、指示なく行わない。
